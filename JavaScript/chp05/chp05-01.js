// Selecting Elements Using ID Attributes

// Selects the element and stores it in a variable
var el = document.getElementById('one');

// changes the value of the class attribute
el.className = 'cool';